# Production entrypoint for Issue #291 Phase A.
#
# Keep this file intentionally tiny: production is the three-unit Phase A
# graph (frontend-edge, control-api, execution-orchestrator), with Postgres
# and QuestDB as private dependencies.  The old ``web-bridge`` monolith is
# not a deployable production target anymore.
name: vnpy-web-bridge-phase-a-prod

include:
  - path: ./docker-compose.phase-a.yml
