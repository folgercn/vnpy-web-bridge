"""Offline MAP producer package for the Phase B artifact plane.

The implementation is intentionally not imported here: ``python -m
map.producer`` must execute without preloading the module and emitting a
``runpy`` warning.  Import public functions from :mod:`map.producer`.
"""
