"""Offline C_FAST allocation producer package for Phase B.

The implementation is imported from :mod:`c_fast_producer.producer` so the
batch module can run directly without a preloaded ``runpy`` target.
"""
